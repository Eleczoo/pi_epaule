{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "type": "debugpy", 
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py", // Specify the path to src/main.py
            "console": "integratedTerminal"
        }
    ]
}